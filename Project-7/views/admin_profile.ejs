<%- include("header") %>
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>user Profile</title>
    <style>
        .user-container {
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 80vh;
        }
        .profile {
            width: 400px; /* Increased width */
            border: 1px solid #ccc;
            padding: 20px;
            border-radius: 10px;
            box-shadow: 0 0 10px rgba(0,0,0,0.1);
            text-align: center;
            background-color: #f9f9f9;
        }
        .profile img {
            width: 150px;
            height: 150px;
            border-radius: 50%;
            margin-bottom: 15px;
        }
        .profile ul {
            list-style: none;
            padding: 0;
        }
        .profile ul li {
            margin: 8px 0;
        }
        .home-link {
            display: inline-block;
            margin-top: 15px;
            text-decoration: none;
            padding: 8px 15px;
            background-color: #007BFF;
            color: white;
            border-radius: 5px;
        }
        .home-link:hover {
            background-color: #0056b3;
        }
    </style>
</head>
<body>
    <div class="user-container">
        <% if (user) { %>
            <div class="profile">
              <h2>Admin Profile</h2>
              <img src="<%= user.profileImage %>" alt="user Image">
              <ul>
                <li><strong>First Name:</strong> <%= user.firstname %></li>
                <li><strong>Last Name:</strong> <%= user.lastname %></li>
                <li><strong>Email:</strong> <%= user.email %></li>
                <li><strong>Password:</strong> <%= user.password %></li>
                <li><strong>Gender:</strong> <%= user.gender %></li>
                <li><strong>Hobbies:</strong> <%= user.hobbies %></li>
                <li><strong>Mobile No:</strong> <%= user.mobileNo %></li>
              </ul>
              <a href="/dashboard" class="home-link">Home Page</a>
            </div>
        <% } else { %>
            <p>user data is not available.</p>
        <% } %>
    </div>
</body>
</html>
<%- include("footer") %>
